import React from "react";

interface Props {
  val: number;
  setVal: React.Dispatch<React.SetStateAction<number>>;
}

const Slider: React.FC<Props> = ({ val, setVal }) => {
  return <div></div>;
};
